if ($title === $status || $quality === $status || $idImdb === $status || $cariLink === $status) {
			return;
		}else{	
			}
			
			
if ($pre === 1) {
    file_put_contents($namaFileSql, "");
  }